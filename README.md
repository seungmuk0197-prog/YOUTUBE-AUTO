Quick notes

- Install system `ffmpeg` for moviepy to render video on Windows. Download from https://ffmpeg.org/ and ensure `ffmpeg.exe` is in your PATH.
- Set `OPENAI_API_KEY` env var (or `config/settings.yaml` -> `tts.api_key`) for TTS.
- Run:

```bash
python main.py
```
